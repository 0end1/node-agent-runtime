{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "P5.4：控制台以 http://localhost:8787 的 remote origin 加载（dev 由 beforeDevCommand 启动 server，release 由 app 自带 node sidecar 启动）。capability 放行该 origin 的 IPC，使 Web 控制台可 invoke Rust 侧自定义命令 check_update / install_update；core:default 覆盖控制台其余 core 能力。应用自定义命令无需额外 permission。",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:8787"]
  },
  "permissions": ["core:default"]
}

{
  "name": "@agent-runtime/policy",
  "version": "0.2.0",
  "description": "C5 policy package of the Agent runtime: authorization decisions (allow/deny/ask) — DefaultPermissionPolicy matrix, PermissionManager approval flow with timeout and always-allowlist, composable policies. Depends on @agent-runtime/types and (type-only) @agent-runtime/sandbox.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "@agent-runtime/sandbox": "0.2.0",
    "@agent-runtime/types": "0.2.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test test/*.test.ts"
  }
}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-agent-runtime.fullname" . }}
  labels:
    {{- include "node-agent-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-agent-runtime.selectorLabels" . | nindent 4 }}

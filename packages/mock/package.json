{
  "name": "@agent-runtime/mock",
  "version": "0.2.0",
  "description": "Mock provider package of the Agent runtime: MockProvider — a deterministic, keyless rule-based model for demos, tests and offline runs. Demo/test asset; kept out of the core to keep the engine lean (M6 P1 review, P0).",
  "license": "MIT",
  "author": {
    "name": "wangzhiyong",
    "email": "y1378379002@gmail.com",
    "url": "https://github.com/0end1"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/0end1/nodeRuntime.git",
    "directory": "packages/mock"
  },
  "homepage": "https://github.com/0end1/nodeRuntime",
  "bugs": {
    "url": "https://github.com/0end1/nodeRuntime/issues"
  },
  "keywords": [
    "agent-runtime",
    "agent",
    "mock",
    "provider",
    "testing"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@agent-runtime/tools-basic": "^0.2.0"
  },
  "devDependencies": {
    "@agent-runtime/core": "^0.2.0",
    "@agent-runtime/types": "^0.2.0"
  },
  "peerDependencies": {
    "@agent-runtime/core": "^0.2.0",
    "@agent-runtime/types": "^0.2.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test test/*.test.ts"
  }
}

{
  "name": "desktop-tauri",
  "private": true,
  "version": "0.1.0",
  "description": "Agent Runtime Console — Desktop shell (Tauri v2) wrapping examples/web",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:sidecar": "node src-tauri/build-server.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.24.0"
  }
}
